@using Pawnshop.Application.EmployeesApplication.Commands.AddEmployee
@using Pawnshop.Application.EmployeesApplication.Commands.DeleteEmployee
@using Pawnshop.Application.EmployeesApplication.Commands.EditEmployee
@using Pawnshop.Application.EmployeesApplication.Dto
@using Pawnshop.Application.EmployeesApplication.Responses
@using Pawnshop.Web.Components.Common
@using Pawnshop.Web.Services
@using Pawnshop.Web.Services.ApiService
@using MudBlazor

@inject ApiService ApiService
@inject ISnackbar Snackbar
@inject LanguageService Lang
@implements IDisposable

<DetailModalOverlay>
    <MudCard Class="w-50" Style="max-height: 85vh; overflow: auto; border-radius: 12px;">
        <MudCardHeader>
            <CardHeaderContent>
                <MudText Typo="Typo.h5" Color="Color.Primary">
                    <b>@Lang.EmployeeManagement - @(SelectedAction == UniversalCrudMenu.CrudAction.Add ? Lang.Add : (SelectedAction == UniversalCrudMenu.CrudAction.Update ? Lang.Update : Lang.Details))</b>
                </MudText>
            </CardHeaderContent>
        </MudCardHeader>

        <MudCardContent>
            @if (SelectedAction == UniversalCrudMenu.CrudAction.Delete)
            {
                <MudText Typo="Typo.h6" Align="Align.Center">@Lang.ConfirmDelete</MudText>
                <MudText Typo="Typo.body1" Align="Align.Center" Class="mt-2">@CurrentEmployee?.Name @CurrentEmployee?.Surname</MudText>
            }
            else
            {
                <MudGrid>
                    <MudItem md="6">
                        <MudTextField @bind-Value="@Name" Label="@Lang.Name" Variant="Variant.Outlined" ReadOnly="@(SelectedAction == UniversalCrudMenu.CrudAction.View)" />
                    </MudItem>
                    <MudItem md="6">
                        <MudTextField @bind-Value="@Surname" Label="@Lang.Surname" Variant="Variant.Outlined" ReadOnly="@(SelectedAction == UniversalCrudMenu.CrudAction.View)" />
                    </MudItem>
                    <MudItem md="12">
                        <MudDatePicker @bind-Date="@BirthDate" Label="@Lang.BirthDate" Variant="Variant.Outlined" ReadOnly="@(SelectedAction == UniversalCrudMenu.CrudAction.View)" />
                    </MudItem>
                </MudGrid>
            }
        </MudCardContent>

        <MudCardActions Class="d-flex justify-space-between px-4 pb-4">
            <MudButton Variant="Variant.Text" Color="Color.Default" OnClick="OnDetailCancel">@Lang.Cancel</MudButton>
            @if (SelectedAction != UniversalCrudMenu.CrudAction.View)
            {
                <MudButton Variant="Variant.Filled" Color="@(SelectedAction == UniversalCrudMenu.CrudAction.Delete ? Color.Error : Color.Primary)" OnClick="HandleCrudAction">
                    @(SelectedAction == UniversalCrudMenu.CrudAction.Delete ? Lang.Delete : Lang.Save)
                </MudButton>
            }
        </MudCardActions>
    </MudCard>
</DetailModalOverlay>

@code {
    [Parameter] public EmployeeDto CurrentEmployee { get; set; }
    [Parameter] public UniversalCrudMenu.CrudAction SelectedAction { get; set; }
    [Parameter] public EventCallback OnDetailCancel { get; set; }
    [Parameter] public EventCallback OnCrudCompleted { get; set; }

    private string Name { get; set; }
    private string Surname { get; set; }
    private DateTime? BirthDate { get; set; }

    protected override void OnInitialized()
    {
        Lang.OnLanguageChanged += StateHasChanged;
        if (CurrentEmployee != null)
        {
            Name = CurrentEmployee.Name;
            Surname = CurrentEmployee.Surname;
            BirthDate = CurrentEmployee.BirthDate;
        }
    }

    private async Task HandleCrudAction()
    {
        try
        {
            if (SelectedAction == UniversalCrudMenu.CrudAction.Add)
            {
                await ApiService.PostAsync<AddEmployeeCommand, AddEmployeeResponse>("Employees/add", new AddEmployeeCommand { Name = Name, Surname = Surname, BirthDate = BirthDate ?? DateTime.Now });
            }
            else if (SelectedAction == UniversalCrudMenu.CrudAction.Update)
            {
                await ApiService.PutAsync<UpdateEmployeeCommand, UpdateEmployeeResponse>("Employees/update", new UpdateEmployeeCommand { EmployeeId = CurrentEmployee.Id, Name = Name, Surname = Surname, BirthDate = BirthDate ?? DateTime.Now });
            }
            else if (SelectedAction == UniversalCrudMenu.CrudAction.Delete)
            {
                await ApiService.DeleteAsync<DeleteEmployeeCommand, DeleteEmployeeResponse>("Employees/delete", new DeleteEmployeeCommand { EmployeeId = CurrentEmployee.Id });
            }

            Snackbar.Add(Lang.SuccessSave, Severity.Success);
            await OnCrudCompleted.InvokeAsync();
            await OnDetailCancel.InvokeAsync();
        }
        catch (Exception ex)
        {
            Snackbar.Add($"{Lang.ErrorGeneric}: {ex.Message}", Severity.Error);
        }
    }

    public void Dispose() => Lang.OnLanguageChanged -= StateHasChanged;
}
